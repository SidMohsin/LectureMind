# LectureMind Frontend

React + Vite student dashboard for LectureMind.

## Run

```
npm install
npm run dev
```

Opens at http://localhost:5173 by default. Set `VITE_API_BASE_URL` in a `.env`
file (see `.env.example`) if your backend runs on a different host/port.
